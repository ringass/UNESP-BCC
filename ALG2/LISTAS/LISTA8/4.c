// A) O algoritmo é O(n), pois pelo codigo conseguimos perceber que dentro do for fazemos o total de duas compracoes por elemento, ou seja 2 * (n-1).

// B) Não é O(n^2) pois precisaria de dois for's encadeados
